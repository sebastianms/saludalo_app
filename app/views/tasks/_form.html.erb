<script src="http://code.jquery.com/ui/1.9.2/jquery-ui.js" type="text/javascript" charset="utf-8"></script>
<script type="text/javascript" language="javascript" charset="utf-8">

//<![CDATA[
$(function (){
            $('#task_start_date').datepicker();
            $('#task_final_date').datepicker();
            });

//]]>
</script>

<%= form_for @task, :html => { :class => 'form-horizontal span6 form_editar_tarea' } do |f| %>
  <%= f.hidden_field :user_id,:value => current_user.id, :class => 'number_field' %>
  <div class="control-group">
    <%= f.label :title, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :title, :class => 'input-block-level' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :description, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :description, :class => 'input-block-level' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :start_date, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :start_date, :class => 'datetime_select input-block-level' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :final_date, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :final_date, :class => 'datetime_select input-block-level' %>
    </div>
  </div>
  <div class="control-group">
    <div class="controls">
      <%= f.hidden_field :cause_id,:value => @task.cause.id, :class => 'number_field input-block-level' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary btn-large' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                tasks_path, :class => 'btn btn-large' %>
  </div>
<% end %>
